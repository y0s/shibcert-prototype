<%= form_for(@cert_state) do |f| %>
  <% if @cert_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cert_state.errors.count, "error") %> prohibited this cert_state from being saved:</h2>

      <ul>
      <% @cert_state.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
